[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "incheck"
version = "0.0.4"
description = "Official Python SDK for the InCheck AI platform."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "InCheck AI", email = "support@incheck.ai" },
]
keywords = ["incheck", "ai", "sdk", "ems", "rag", "documents", "chat"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "httpx>=0.27",
    "pydantic>=2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "respx>=0.21",
    "ruff>=0.5",
]

[project.urls]
Homepage = "https://github.com/incheckai/incheck-python"
Documentation = "https://docs.incheck.ai"
Issues = "https://github.com/incheckai/incheck-python/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/incheck"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/README.md",
    "/LICENSE",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
